# Implementation Patterns

<!-- Knowledge accumulated from completed implementations.
     Organized by domain skill tag. Each entry includes the date it was
     discovered and the feature where it was learned.

     Format:
     ## domain-tag

     ### Pattern title (YYYY-MM-DD)
     Description of the pattern — what it is, when it applies, and why
     it matters. Include enough context that a planner in a future session
     can understand whether this pattern is relevant to their feature.
     Discovered during FEAT-NNN implementation.
-->
